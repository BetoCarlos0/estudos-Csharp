using estudos_Csharp.arquivos.Helper;

namespace estudos_Csharp.arquivos
{
    public class Program
    {/*
        static void Main(string[] args)
        {
            //var path = Path.Combine(Environment.CurrentDirectory);
            var conteudo = new List<string>{"linha 1", "linha 2", "linha 3"};
            var conteudo2 = new List<string>{"linha 4", "linha 5", "linha 6"};
            var paths = new FileHelper();
            //paths.ListDirectory(path);

            //var paths = new FileHelper();
            //paths.ListFiles(path);

            var file = Path.Combine(Environment.CurrentDirectory, "arquivos", "teste");
            var file2 = Path.Combine(file, "textoStream.txt");
            //paths.CriarDirec(file);
            //paths.CreateFile(file2, "teste de escrita");
            //paths.DeletDirec(file);
            //paths.CreateFilestream(file2, conteudo);
            //paths.AddFilestream(file2, conteudo2);
            paths.ReadFileStream(file2);
        }*/
    }
}